<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.randomeveryday">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Randomeveryday">
        <activity android:name=".SplashActivity"></activity>
        <activity android:name=".MapsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SouthActivity" />
        <activity android:name=".CentralActivity" />
        <activity android:name=".NortheasternActivity" />
        <activity android:name=".ThenorthActivity" />
        <activity android:name=".RegionmenuActivity" />
        <activity android:name=".MenurecipeActivity" />
        <activity android:name=".bakedfoodActivity" />
        <activity android:name=".boiledfoodActivity" />
        <activity android:name=".fryfoodActivity" />
        <activity android:name=".RecipedrinkActivity" />
        <activity android:name=".RecipefoodActivity" />
        <activity android:name=".drinkActivity" />
        <activity android:name=".RandomMenu" />
        <activity android:name=".CookActivity" />
        <activity android:name=".Menuhome" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>